{
  "homepage": "https://tibule12.github.io/AutoPromote/",
  "name": "autopromote-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "firebase": "^12.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "clean-docs": "rimraf docs",
  "build": "react-scripts build && rimraf ../docs && cpy build/**/* ../docs --parents",
  "build:win": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
  "deploy": "git add ../docs && git commit -m 'Update docs for GitHub Pages' && git push"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "rimraf": "^3.0.2",
    "cpy-cli": "^4.2.0"
  }
}
