{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Local package.json schema (permissive)",
  "description": "Local workspace schema to avoid remote fetch failures.",
  "type": "object",
  "additionalProperties": true
}
