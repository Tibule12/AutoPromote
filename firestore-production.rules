rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // LAUNCH DAY: Allow all authenticated users to read and write all collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}